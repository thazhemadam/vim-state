{
  "name": "vim-state-monorepo",
  "version": "0.1.0",
  "private": true,
  "license": "AGPL-3.0-only",
  "description": "Vim state engine and host integrations.",
  "type": "module",
  "workspaces": [
    "packages/vim-state",
    "packages/integrations/*"
  ],
  "pi": {
    "extensions": [
      "./packages/integrations/pi-vim/src/index.ts"
    ]
  },
  "scripts": {
    "build": "npm run build --workspace vim-state",
    "check": "npm run build && npm run check --workspaces --if-present",
    "clean": "rm -rf packages/vim-state/dist",
    "lint": "npm run lint --workspaces --if-present",
    "lint:fix": "npm run lint:fix --workspaces --if-present",
    "test": "npm run build && npm run test --workspaces --if-present",
    "prepare": "npm run build",
    "dev:pi": "npm run build && VIM_PI_PREFILL='Lorem ipsum dolor sit amet, consectetur adipiscing elit.\\nSed do eiusmod tempor incididunt ut labore et dolore magna aliqua.' PI_CODING_AGENT_DIR=$PWD/.pi-dev/agent PI_CODING_AGENT_SESSION_DIR=$PWD/.pi-dev/sessions pi --no-extensions -e .",
    "dev:pi:clean": "rm -rf .pi-dev",
    "format": "prettier --write ."
  },
  "dependencies": {
    "typescript": "^5.9.3"
  },
  "devDependencies": {
    "oxlint": "^1.70.0",
    "prettier": "^3.8.4",
    "tsx": "^4.22.4"
  }
}
