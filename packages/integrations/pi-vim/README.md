# pi-vim

Vim-style modal editing for [Pi](https://pi.dev).

This private workspace contains the Pi-specific integration for [`vim-state`](../../vim-state). Pi loads the TypeScript extension from `src/index.ts`.

For local development from the monorepo root:

```bash
npm install
npm run dev:pi
```
